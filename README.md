# ACTYS
This is version 1.2 of actys code
User is suggested to first run "first_run.sh" file from ACTYS main directory to set the ACTYS_HOME variable in the .bashrc file.
After first_run is over kindly either open new terminal to use ACTYS or do source ~/.bashrc for registering
ACTYS_HOME variable.

